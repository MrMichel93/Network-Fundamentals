# 📊 Module 03: Docker Fundamentals - Diagrams

Visual representations to help understand Docker architecture, containers, images, and networking.

## 1. Docker Architecture

High-level overview of Docker components and their relationships:

```
┌─────────────────────────────────────────┐
│         Docker Host                      │
│  ┌─────────────────────────────────┐   │
│  │     Docker Daemon               │   │
│  │     (dockerd)                   │   │
│  │                                 │   │
│  │  Manages:                       │   │
│  │  - Images                       │   │
│  │  - Containers                   │   │
│  │  - Networks                     │   │
│  │  - Volumes                      │   │
│  └────────┬────────────────────────┘   │
│           │              │               │
│      ┌────▼───┐     ┌───▼────┐         │
│      │Container│    │Container│         │
│      │   App1  │    │  App2  │         │
│      │         │    │        │         │
│      │ ┌─────┐ │    │ ┌────┐│         │
│      │ │ App │ │    │ │App ││         │
│      │ └─────┘ │    │ └────┘│         │
│      │  Libs   │    │  Libs │         │
│      └─────────┘    └────────┘         │
│                                          │
│  ┌──────────┐  ┌──────────┐           │
│  │  Image   │  │  Image   │           │
│  │  nginx:  │  │ python:  │           │
│  │  latest  │  │   3.9    │           │
│  └──────────┘  └──────────┘           │
│                                          │
│  ┌──────────────────────────────────┐  │
│  │         Docker Volumes           │  │
│  │  Persistent data storage         │  │
│  └──────────────────────────────────┘  │
│                                          │
│  ┌──────────────────────────────────┐  │
│  │       Docker Networks            │  │
│  │  Container communication         │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
           ▲
           │
           │ Docker CLI
           │ (docker commands)
           │
    ┌──────┴──────┐
    │    User     │
    │  Terminal   │
    └─────────────┘
```

**Component Interaction:**
```
User → Docker CLI → Docker Daemon → Container Runtime
  │                      │
  │                      ├──→ Pull/Build Images
  │                      ├──→ Create Containers
  │                      ├──→ Manage Networks
  │                      └──→ Handle Volumes
  │
  └──→ docker run, docker build, docker ps, etc.
```

---

## 2. Container vs Virtual Machine

Side-by-side comparison showing the architectural differences:

```
┌────────────────────────────────┐  ┌────────────────────────────────┐
│     Virtual Machines           │  │         Containers             │
└────────────────────────────────┘  └────────────────────────────────┘

VIRTUAL MACHINES:                   CONTAINERS:
────────────────                    ───────────

┌──────────────────────────────┐  ┌──────────────────────────────┐
│         Application          │  │         Application          │
├──────────────────────────────┤  ├──────────────────────────────┤
│           Binaries           │  │           Binaries           │
│          Libraries           │  │          Libraries           │
├──────────────────────────────┤  ├──────────────────────────────┤
│       Guest OS (Linux)       │  │      (Shared Kernel)         │
│         Full OS Stack        │  │                              │
├──────────────────────────────┤  ├──────────────────────────────┤
│         Application          │  │         Application          │
├──────────────────────────────┤  ├──────────────────────────────┤
│           Binaries           │  │           Binaries           │
│          Libraries           │  │          Libraries           │
├──────────────────────────────┤  ├──────────────────────────────┤
│      Guest OS (Windows)      │  │      (Shared Kernel)         │
│         Full OS Stack        │  │                              │
├══════════════════════════════┤  ├══════════════════════════════┤
│        Hypervisor            │  │       Docker Engine          │
│    (VirtualBox, VMware)      │  │       Container Runtime      │
├══════════════════════════════┤  ├══════════════════════════════┤
│      Host Operating System   │  │   Host Operating System      │
├══════════════════════════════┤  ├══════════════════════════════┤
│      Physical Hardware       │  │    Physical Hardware         │
└──────────────────────────────┘  └──────────────────────────────┘

    Each VM = GB of disk space      Each container = MB
    Boot time = minutes              Boot time = seconds
    Resource overhead = High         Resource overhead = Low
    Isolation = Strong               Isolation = Process-level
    Portability = Lower              Portability = Higher


COMPARISON TABLE:
─────────────────

┌──────────────────┬────────────────┬─────────────────┐
│   Characteristic │   Virtual Machines   Containers  │
├──────────────────┼────────────────┼─────────────────┤
│ Size             │   GBs          │   MBs           │
│ Startup Time     │   Minutes      │   Seconds       │
│ Resource Usage   │   High         │   Low           │
│ Isolation        │   Full OS      │   Process       │
│ Density          │   Low (10s)    │   High (100s)   │
│ Portability      │   Lower        │   Higher        │
│ OS Support       │   Multiple OSes│   Same kernel   │
└──────────────────┴────────────────┴─────────────────┘


USE CASES:
──────────

VMs - Better for:                  Containers - Better for:
├── Running different OSes         ├── Microservices
├── Strong isolation needed        ├── CI/CD pipelines
├── Legacy applications            ├── Cloud-native apps
└── Full system simulation         ├── Quick scaling
                                   └── DevOps workflows
```

---

## 3. Dockerfile Build Process

Step-by-step visualization of how Docker builds images from a Dockerfile:

```
Dockerfile:                         Image Layers:
───────────                         ─────────────

FROM ubuntu:20.04            ─────► ┌─────────────────────┐
                                    │ Base: ubuntu:20.04  │ Layer 1
                                    │ Size: 72.8 MB       │ (Read-only)
                                    └─────────────────────┘
                                              ▲
RUN apt-get update &&        ─────►          │
    apt-get install -y python3               │
                                    ┌─────────────────────┐
                                    │ RUN apt-get update  │ Layer 2
                                    │ Size: 45 MB         │ (Read-only)
                                    └─────────────────────┘
                                              ▲
WORKDIR /app                 ─────►          │
                                             │
                                    ┌─────────────────────┐
                                    │ WORKDIR /app        │ Layer 3
                                    │ Size: 0 MB          │ (Read-only)
                                    └─────────────────────┘
                                              ▲
COPY requirements.txt .      ─────►          │
                                             │
                                    ┌─────────────────────┐
                                    │ COPY requirements   │ Layer 4
                                    │ Size: 0.5 KB        │ (Read-only)
                                    └─────────────────────┘
                                              ▲
RUN pip install -r           ─────►          │
    requirements.txt                         │
                                    ┌─────────────────────┐
                                    │ RUN pip install     │ Layer 5
                                    │ Size: 120 MB        │ (Read-only)
                                    └─────────────────────┘
                                              ▲
COPY . .                     ─────►          │
                                             │
                                    ┌─────────────────────┐
                                    │ COPY source code    │ Layer 6
                                    │ Size: 10 MB         │ (Read-only)
                                    └─────────────────────┘
                                              ▲
CMD ["python3", "app.py"]    ─────►          │
                                             │
                                    ┌─────────────────────┐
                                    │ CMD metadata        │ Layer 7
                                    │ Size: 0 MB          │ (Read-only)
                                    └─────────────────────┘
                                              │
                                              │
                                    ┌─────────▼───────────┐
                                    │  Final Image:       │
                                    │  myapp:latest       │
                                    │  Total: ~248 MB     │
                                    └─────────────────────┘


Build Process with Caching:
────────────────────────────

Build #1 (Fresh):          Build #2 (With cache):
                          
FROM ubuntu:20.04          FROM ubuntu:20.04
└─ Pulling...             └─ Using cache ✓
RUN apt-get update         RUN apt-get update
└─ Running...             └─ Using cache ✓
COPY requirements.txt      COPY requirements.txt
└─ Copying...             └─ Using cache ✓
RUN pip install           RUN pip install
└─ Installing...          └─ Using cache ✓
COPY . .                  COPY . .                    ← Changed!
└─ Copying...             └─ Rebuilding...           ← From here
CMD ["python3", "app.py"]  CMD ["python3", "app.py"]  ← down
└─ Setting...             └─ Rebuilding...


Layer Reuse Example:
────────────────────

Image 1: myapp:v1         Image 2: myapp:v2
┌─────────────────┐       ┌─────────────────┐
│ App code v1     │       │ App code v2     │ ← New layer
├─────────────────┤       ├─────────────────┤
│ Dependencies    │───────│ Dependencies    │ ← Shared
├─────────────────┤  ▲    ├─────────────────┤
│ Python 3.9      │  │    │ Python 3.9      │ ← Shared
├─────────────────┤  │    ├─────────────────┤
│ Ubuntu base     │  │    │ Ubuntu base     │ ← Shared
└─────────────────┘  └────└─────────────────┘
                     Layers reused from disk

Disk usage: 248 MB + 10 MB = 258 MB
(Not 248 MB × 2 = 496 MB!)
```

**Best Practices for Layer Optimization:**
```
❌ Bad (invalidates cache):        ✅ Good (efficient caching):

COPY . .                           COPY requirements.txt .
RUN pip install -r requirements    RUN pip install -r requirements.txt
                                   COPY . .

Every code change rebuilds all     Only code changes rebuild last layer
```

---

## 4. Docker Networking Modes

Diagram showing different network configurations:

```
1. BRIDGE NETWORK (Default):
─────────────────────────────

┌─────────────────────────────────────────────────┐
│              Docker Host                        │
│                                                 │
│  ┌──────────────────────────────────────┐      │
│  │      docker0 bridge                  │      │
│  │      172.17.0.1                      │      │
│  └───┬─────────────┬──────────────┬─────┘      │
│      │             │              │             │
│  ┌───▼─────┐  ┌────▼────┐  ┌─────▼────┐       │
│  │Container│  │Container│  │Container │       │
│  │  .0.2   │  │  .0.3   │  │  .0.4    │       │
│  └─────────┘  └─────────┘  └──────────┘       │
│                                                 │
│  Containers can talk to each other             │
│  NAT to access external network                │
└───────────────────────┬─────────────────────────┘
                        │
                        │ NAT
                        ▼
                  External Network


2. HOST NETWORK:
────────────────

┌─────────────────────────────────────────────────┐
│              Docker Host                        │
│           Network: 192.168.1.100                │
│                                                 │
│  ┌───────────────────────────────────┐         │
│  │         Container                 │         │
│  │   Uses host network directly      │         │
│  │   No network isolation             │         │
│  │   Port 80 = Host's port 80        │         │
│  └───────────────────────────────────┘         │
│                                                 │
│  Container shares host's network stack         │
│  Best performance, least isolation             │
└─────────────────────────────────────────────────┘


3. NONE NETWORK:
────────────────

┌─────────────────────────────────────────────────┐
│              Docker Host                        │
│                                                 │
│  ┌───────────────────────────────────┐         │
│  │         Container                 │         │
│  │    No network interfaces          │         │
│  │    Complete isolation             │         │
│  │    Only loopback (127.0.0.1)      │         │
│  └───────────────────────────────────┘         │
│                                                 │
│  No network access                             │
│  Maximum security                              │
└─────────────────────────────────────────────────┘


4. CUSTOM BRIDGE NETWORK:
──────────────────────────

┌─────────────────────────────────────────────────┐
│              Docker Host                        │
│                                                 │
│  ┌──────────────────────────────────────┐      │
│  │   my-network (custom bridge)         │      │
│  │   172.20.0.0/16                      │      │
│  └───┬─────────────┬──────────────┬─────┘      │
│      │             │              │             │
│  ┌───▼─────┐  ┌────▼────┐  ┌─────▼────┐       │
│  │web      │  │api      │  │db        │       │
│  │(nginx)  │  │(node)   │  │(postgres)│       │
│  └─────────┘  └─────────┘  └──────────┘       │
│                                                 │
│  Containers use DNS names                      │
│  Better isolation than default bridge          │
└─────────────────────────────────────────────────┘


5. OVERLAY NETWORK (Multi-host):
─────────────────────────────────

┌─────────────────────┐       ┌─────────────────────┐
│    Docker Host 1    │       │    Docker Host 2    │
│  ┌───────────────┐  │       │  ┌───────────────┐  │
│  │  Container A  │  │       │  │  Container C  │  │
│  └───────┬───────┘  │       │  └───────┬───────┘  │
│          │          │       │          │          │
│    ┌─────▼──────┐   │       │   ┌──────▼─────┐   │
│    │  Overlay   │───┼───────┼───│  Overlay   │   │
│    │  Network   │   │       │   │  Network   │   │
│    └─────┬──────┘   │       │   └──────┬─────┘   │
│          │          │       │          │          │
│  ┌───────▼───────┐  │       │  ┌───────▼───────┐  │
│  │  Container B  │  │       │  │  Container D  │  │
│  └───────────────┘  │       │  └───────────────┘  │
└─────────────────────┘       └─────────────────────┘
         │                             │
         └──────── Network ────────────┘
         
    All containers can communicate
    across different hosts
    Used in Docker Swarm/Kubernetes


Network Mode Comparison:
─────────────────────────

┌────────────┬──────────┬──────────┬───────────┐
│    Mode    │Performance│Isolation │Use Case   │
├────────────┼──────────┼──────────┼───────────┤
│ Bridge     │  Good    │  Good    │ Default   │
│ Host       │  Best    │  None    │ Performance│
│ None       │  N/A     │  Maximum │ Security  │
│ Custom     │  Good    │  Better  │ Multi-app │
│ Overlay    │  Good    │  Good    │ Swarm     │
└────────────┴──────────┴──────────┴───────────┘
```

---

## 5. Docker Compose Multi-Container Setup

Showing how services interact in a Docker Compose application:

```
docker-compose.yml:
───────────────────

version: '3.8'
services:
  web:
    image: nginx
    ports:
      - "80:80"
    depends_on:
      - api
  
  api:
    build: ./api
    depends_on:
      - db
      - redis
  
  db:
    image: postgres:13
    volumes:
      - db-data:/var/lib/postgresql/data
  
  redis:
    image: redis:alpine


Runtime Architecture:
─────────────────────

┌─────────────────────────────────────────────────────┐
│                Docker Compose                       │
│           myapp_network (bridge)                    │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │              web (nginx)                    │   │
│  │          Container: myapp_web_1             │   │
│  │          Port: 80:80                        │   │
│  └──────────┬──────────────────────────────────┘   │
│             │                                       │
│             │ HTTP requests                         │
│             │                                       │
│  ┌──────────▼──────────────────────────────────┐   │
│  │              api (node.js)                  │   │
│  │          Container: myapp_api_1             │   │
│  │          Port: 3000 (internal)              │   │
│  └──────┬─────────────────────────┬────────────┘   │
│         │                         │                 │
│         │                         │                 │
│  ┌──────▼──────────┐       ┌──────▼──────────┐    │
│  │  db (postgres)  │       │  redis (cache)  │    │
│  │  myapp_db_1     │       │  myapp_redis_1  │    │
│  │  Port: 5432     │       │  Port: 6379     │    │
│  │  (internal)     │       │  (internal)     │    │
│  └──────┬──────────┘       └─────────────────┘    │
│         │                                          │
│         │ Volume: db-data                          │
│         ▼                                          │
│  ┌──────────────────┐                             │
│  │   Docker Volume   │                             │
│  │   db-data         │                             │
│  │   (persistent)    │                             │
│  └───────────────────┘                             │
└─────────────────────────────────────────────────────┘
         │
         │ Port mapping: 80
         ▼
    External Access
    (http://localhost)


Service Communication:
──────────────────────

User → web:80 → api:3000 → db:5432
              → redis:6379

Service Discovery (DNS):
├── web can reach: api, db, redis
├── api can reach: db, redis
└── db and redis: isolated

Container Names:
├── myapp_web_1
├── myapp_api_1
├── myapp_db_1
└── myapp_redis_1


Startup Order (depends_on):
────────────────────────────

1. db and redis start (no dependencies)
       │
       ▼
2. api starts (waits for db and redis)
       │
       ▼
3. web starts (waits for api)


Volume Types:
─────────────

Named Volume:           Bind Mount:
┌──────────────┐       ┌──────────────┐
│ Docker       │       │ Host         │
│ managed      │       │ directory    │
│              │       │              │
│ db-data      │       │ ./app:/app   │
└──────────────┘       └──────────────┘
Persistent data        Development
Managed by Docker      Direct file access


Lifecycle Commands:
───────────────────

$ docker-compose up
  ├─ Create network
  ├─ Create volumes
  ├─ Build/pull images
  └─ Start containers

$ docker-compose down
  ├─ Stop containers
  ├─ Remove containers
  ├─ Remove network
  └─ (Keep volumes)

$ docker-compose down -v
  └─ Also remove volumes
```

---

## Summary

These diagrams illustrate:
- ✅ Docker architecture with daemon, images, and containers
- ✅ Fundamental differences between containers and VMs
- ✅ Image layer building and caching mechanisms
- ✅ Various Docker networking modes and their use cases
- ✅ Multi-container orchestration with Docker Compose
- ✅ Service dependencies and communication patterns

**Key Concepts:**
- **Images**: Read-only templates with layers
- **Containers**: Running instances of images
- **Volumes**: Persistent data storage
- **Networks**: Container communication
- **Compose**: Multi-container orchestration

**Common Commands:**
```
# Container management
docker run, docker ps, docker stop, docker rm

# Image management
docker build, docker pull, docker push, docker images

# Network management
docker network create, docker network ls

# Volume management
docker volume create, docker volume ls

# Compose commands
docker-compose up, docker-compose down, docker-compose logs
```

**Next Steps:**
- Build your first Dockerfile
- Create a multi-container application
- Practice with different network modes
- Explore volume persistence
- Complete the [exercises](./exercises.md)
